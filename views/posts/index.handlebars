<div class="card">
    <div class="card-body">
        <h1 class="card-title">{{post.title}}</h1>
        <hr>
        <small>Publication date: {{post.created_at}}</small>
        <br>
        {{post.content}}
    </div>
</div>

{{>_goBackButton}}